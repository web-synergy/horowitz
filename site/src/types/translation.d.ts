export enum Translations {
  SEARCH = 'search',
  ADDRESS = 'address',
  PHONE = 'phone',
  PRESS_CENTER = 'pressCenter',
}

export enum NotFound {
  TITLE = 'title',
  TEXT = 'text',
  BTN = 'btn',
}
