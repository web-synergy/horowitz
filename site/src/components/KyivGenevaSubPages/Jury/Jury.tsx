import { Container, Typography } from '@mui/material';
import PageTemplate from '@/components/Common/PageTemplate';

const Jury = () => {
  return (
    <PageTemplate>
      <Container>
        <Typography>Single Jury</Typography>
      </Container>
    </PageTemplate>
  );
};

export default Jury;
